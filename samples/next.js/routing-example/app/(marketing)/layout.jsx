export default function MarketingLayout({ children }) {
  return (
    <div className="card">
      <p><span className="code">(marketing)</span> route group layout</p>
      {children}
    </div>
  );
}
