# Search Autocomplete (Next.js)

## Overview

A client-side autocomplete sample that demonstrates debouncing user input and aborting in-flight requests with `AbortController`.

## Run It Locally

```bash
cd samples/next.js/search-autocomplete
npm install
npm run dev
```

Open http://localhost:3000
